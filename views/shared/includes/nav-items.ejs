<ul class="nav-items">
  <% if (!locals.isAdmin && locals.isAuth) { %>
    <li><a href="/adddoctor">Add Doctors</a></li>
    <li><a href="/meeting/new">New Meeting</a></li>
  <% } %>

  <% if (locals.isAuth && !locals.isAdmin) { %>
    <li><a href="/activity">Activity</a></li>
  <% } %>

  <% if (locals.isAdmin) { %>
    <li><a href="/admin/doctors">Manage Doctors</a></li>
    <li><a href="/admin/reps">Manage Reps</a></li>
    <li><a href="/admin/activity">Recent Activity</a></li>
  <% } %>

  <% if (!locals.isAuth) { %>
    <!-- <li><a href="/signup">Signup</a></li> -->
    <li><a href="/login">Login</a></li>
  <% } %>

  <% if (locals.isAuth) { %>
    <li>
      <form action="/logout" method="POST">
        <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
        <button>Logout</button>
      </form>
    </li>
  <% } %>
</ul>